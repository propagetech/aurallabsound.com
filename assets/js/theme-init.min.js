/* Theme initialization - runs before page render.
   Default is dark; light only when the visitor chooses via the theme toggle. */
(function(){const root=document.documentElement;const savedTheme=localStorage.getItem("theme-preference");root.setAttribute("data-theme",savedTheme==="light"?"light":"dark")})();